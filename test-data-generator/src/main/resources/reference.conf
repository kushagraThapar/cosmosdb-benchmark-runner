benchmark {
   dataGenerator {
      //benchmark related configs
      reCreateCollections = true
      generateTestData = true
      performTestDataValidation = false

      collectionRusOnCreate = 1600000
      collectionRusPostCreate = 1600000
      createWithConsistencyLevel = BoundedStaleness

      collectionPrefix = ""

      testGraphCount = 10000
      nodesPerGraph = 6

      generatorType = "two_table_simple"
    }
}